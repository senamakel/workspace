# Shared tmux behavior. Host-specific colors are applied by workspace-tmux.

# The outer/master session keeps tmux's standard prefix. Dedicated remote
# workspace servers override this to C-z after loading this file.
set-option -g prefix C-b
unbind-key C-z
bind-key C-b send-prefix

# Mouse support makes windows, panes, resizing, and scrollback clickable.
set-option -g mouse on
set-option -g focus-events on
set-option -g set-clipboard on
set-option -g allow-passthrough on
set-option -g history-limit 100000

# Make copies from a nested tmux reach the laptop's clipboard. set-clipboard is
# not enough on its own: tmux only emits the OSC 52 clipboard escape when its
# own terminal advertises the Ms capability, and the TERM a tmux pane exports
# (tmux-256color, screen-256color) has no Ms. So an inner tmux running on a box
# decides its "terminal" — the outer tmux — cannot take a clipboard write, and
# the copy stops at the inner tmux's paste buffer. Declaring the feature for
# every TERM lets the inner tmux emit OSC 52, the outer tmux forward it, and
# the local terminal apply it.
set-option -as terminal-features ',*:clipboard'

# Keep inactive panes dark while allowing the active pane to use the terminal's
# native background. workspace-tmux supplies each host's accent colors.
set-option -g pane-border-status top
set-option -g pane-border-format ' #{pane_title} '
set-option -g pane-border-style bg=colour0
set-option -g pane-active-border-style bg=colour0
set-window-option -g window-active-style bg=terminal
set-window-option -g window-style bg=colour0
